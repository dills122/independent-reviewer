# MVP batch 2 — 2026-09-09

Prompt v6 clarifies that rules violated by one defect belong in one finding,
and explains how merged findings retain their preliminary references. No new
validation layers or tests. Existing token-reservation fixture allowance was
updated for prompt length. `npm run check` passes all 190 existing tests.

## Duplicate-finding fix

First logging rerun produced one final finding but failed reconciliation references,
even after repair. Clarifying MERGED versus WITHDRAWN yielded one P1 logging
finding in two calls without repair (92.1 seconds). Both attempts are retained.
Reported cost for these two runs: $0.001609835.

## Five-case batch

Same GPT-OSS 120B configuration, $0.02 admission ceiling per review. Expectations
and direct before/after execution results were stored outside each fixture and
were not supplied to the reviewer. Author explanation remained withheld until
preliminary assessment was persisted.

| Scenario | Observed result | Calls | Seconds | Reported cost |
| --- | --- | --- | --- | --- |
| Clean multi-file refactor | Ready, no findings | 2 | 62.8 | $0.000483130 |
| Cross-file currency-unit bug | One P1; correctly identifies 100× overcharge | 3 | 150.1 | $0.001192760 |
| Two unrelated boundary bugs | Both found; pagination omission and shipping threshold | 2 | 132.5 | $0.000813340 |
| Bug with misleading author explanation | One P1; rejects unsupported author justification | 2 | 151.1 | $0.000732833 |
| Correct code with mistaken author concern | Ready, no findings; explains why concern is wrong | 2 | 109.6 | $0.000512711 |

All five completed with expected findings, no observed false positives, and no
duplicate findings. Cross-file review needed one repair for a finding-origin
reference mismatch; it did not require a code change or a fresh review. Four
reviews completed on their first final response. This remains a small synthetic
sample, not a general accuracy estimate.

Five-case reported cost: **$0.003734774** across 11 calls.
Including both logging reruns: **$0.005344609** across 16 calls.
Costs are provider telemetry, not settled billing. Both CoreWeave and DeepInfra
served calls through the existing allowed routing configuration.

## Reproduce or inspect

Private requests, fixture repositories, expected outcomes, runner observations,
provider responses, and reports are under `.review-runs/batch2-2026-09-09/`.
The successful logging recheck is under
`.review-runs/batch2-logging-recheck-2026-09-09/`.

From repository root, use the normal CLI with a new output directory:

```sh
node --env-file=.env dist/src/cli.js review \
  --request .review-runs/batch2-2026-09-09/cross_file/request.json \
  --config examples/review-config.gpt-oss-120b.json \
  --output .review-runs/manual-cross-file-01
```

Other scenario directories: `clean_multi`, `two_bugs`, `misleading_author`,
`wrong_concern`. Exit 2 means a completed Not ready review, not execution failure.
