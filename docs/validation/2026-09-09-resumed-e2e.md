# Resumed live E2E check — 2026-09-09

Tested the inherited, uncommitted candidate-v2 / prompt-v9 implementation on
`codex/project-health-e2e-check`. `npm run build` passed. No runtime changes or
new tests were made during this check.

## Results

| Fixture / attempt | Outcome | Calls | Repair calls | Reported cost |
| --- | --- | --- | --- | --- |
| Eight-file clean order refactor | READY, no findings, all eight paths inspected | 2 | 0 | $0.001000398 |
| Logging guidance violation | NOT_READY, one combined finding for identifier logging / side effects | 2 | 0 | $0.000655600 |
| Eight-file order bugs, original route | DeepInfra returned null content, stop, no usage | 1 | 0 | Unknown |
| Eight-file order bugs, fresh run | Transport timeout at 180 seconds; submission outcome uncertain | 1 | 0 | Unknown |
| Eight-file order bugs, CoreWeave-only diagnosis | Explicit 429, rate_limit_exceeded, upstream_provider_shared_pool; retry denied by token reservation | 1 | 0 | Unknown |

The two successful reviews persisted their preliminary assessment before author
delivery and completed without candidate rejection or repair. Clean review took
about 159 seconds; logging review about 153 seconds. The buggy fixture did not
produce an assessment, so defect detection is unmeasured in this batch.

Total provider-reported cost is $0.001655998, excluding three failed calls with
unknown usage. Five network-enabled reviews made seven calls. Initial sandboxed
launches of the two larger fixtures also failed with uncertain transport before
any response; those artifacts remain separately recorded. A steering launch
with an incorrect request filename failed locally without a provider call.

## Interpretation

Current evidence separates useful completed reviews from unreliable delivery.
The model produced the expected clean and guidance results. An empty completion,
a timeout, and an explicit shared-pool 429 do not measure reasoning quality.
They show that this model/routing configuration is unreliable in this batch.

The retry implementation did encounter a qualifying live 429, but the larger
fixture's conservative reservation plus the unknown-usage failed call exceeded
the existing 80,000-token cap. No retry was sent. This batch therefore does not
prove live retry recovery. The $0.02 per-review cap and 180-second timeout stayed
unchanged; the final diagnosis changed only the endpoint allowlist to CoreWeave.

No broader paid matrix was launched after these failures. Before declaring the
inherited patch complete, its documentation and outstanding local assertion
failure still need closure: the previous task's saved suite log reports 192/193
passing, with a blind-input assertion matching the policy's literal
`AUTHOR_PACKET` text. That suite was not rerun here.

Private evidence: `.review-runs/resumed-e2e-2026-09-09/`, including per-attempt
run ledgers, redacted provider bodies, frozen packets, and successful reports.
